<%= form_for([:story, @chapter]) do |f| %>
    <% if @chapter.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@chapter.errors.count, "error") %> prohibited this story from being saved:</h2>)
          <ul>
            <% @chapter.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field has-success">
      <%= f.label :user_text, class: 'control-label'%><br>
      <%= f.text_area :user_text, class: 'form-control', rows: '20', style: 'max-width:500px', placeholder: 'Textarea' %><br>
    </div>
    <div class="field has-success">
      <%# f.label :user_story, class: 'control-label' %><br>
      <%# f.file_field :user_story %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>