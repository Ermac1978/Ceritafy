<%= form_for(@story) do |f| %>
  <% if @story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@story.errors.count, "error") %> prohibited this story from being saved:</h2>

      <ul>
      <% @story.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field has-success">
    <%= f.label :title, class: 'control-label'%><br>
    <%= f.text_field :title, class: 'form-control', style: 'max-width:500px' %><br>
  </div>
  <div class="field has-success">
    <%= f.label :user_text, class: 'control-label'%><br>
    <%= f.text_area :user_text, class: 'form-control', rows: '30', style: 'max-width:800px', placeholder: 'Textarea' %><br>
  </div>
  <div class="field has-success">
    <%= f.label :user_story, class: 'control-label' %><br>
    <%= f.file_field :user_story %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
